[package]
name = "cadx-app"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cadx-agent = { path = "../cadx-agent" }
cadx-core = { path = "../cadx-core" }
eframe = { version = "0.31.1", default-features = false, features = ["default_fonts", "glow"] }

[lints]
workspace = true
